# Task Environment

This environment provides a local skills library mounted on the filesystem.

Read the task instruction first, inspect the workspace, and use local skills when helpful.
Do not rely on `graphskills-query` or any Graph of Skills runtime command.

The mounted skill library is available at `/opt/skillsbench/skills`, `/root/skills`, and `/app/skills`.
